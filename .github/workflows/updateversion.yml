name: .NET Core

on:
  push:
    tags: 
      - "v*"


jobs:
  update-majorver:
    name: Update major version tag
    runs-on: ubuntu-test
    steps:
      - uses: nowactions/update-majorver@v1
        with:
          github_token: ${{ secrets. GITHUB_TOKEN }}
